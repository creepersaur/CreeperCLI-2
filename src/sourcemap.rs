// use std::collections::HashMap;

/*
pub fn generate_sourcemap(name: String, ) {
    let mut map: HashMap<&str, _> = HashMap::new();

    map.insert("className", "DataModel");
    map.insert("name", name.as_str());

    let mut children: Vec<_> = vec![];

    children.push(HashMap::new());

    map.insert("children", children);
}
*/